# Mech-MSR 2D 算法步骤参考

> 来源: https://docs.mech-mind.net/zh/3d-measurement-manual/latest/software-steps/steps.html (V2.2.0)
>
> 本文档包含步骤库中所有 2D 相关的算法步骤说明，你可以使用。

## 重要规则

1. **优先使用本文档中列出的算法步骤**：回答用户问题时，必须优先从本文档提供的算法步骤中选择合适的方案。推荐的算法流程应尽量由本文档中已有的步骤组成。
2. **标注非文档内容**：如果本文档中没有能直接解决用户问题的步骤，而你需要推荐文档外的算法或方法，必须明确标注"【注意：以下建议超出本系统已有步骤，仅为参考猜测】"，让用户清楚区分哪些是系统已支持的能力、哪些是额外建议。
3. **不要编造不存在的步骤名称**：只使用本文档中明确列出的步骤名称，不要杜撰类似名称的步骤。

---

## 目录

1. [数据采集](#数据采集)
2. [预处理 - 基本处理](#预处理---基本处理)
3. [预处理 - 颜色处理](#预处理---颜色处理)
4. [预处理 - 掩膜](#预处理---掩膜)
5. [预处理 - 空间变换](#预处理---空间变换)
6. [定位 - 2D几何特征](#定位---2d几何特征)
7. [定位 - 2D定位](#定位---2d定位)
8. [识别](#识别)
9. [后处理 - 2D分析](#后处理---2d分析)
10. [测量 - 2D测量](#测量---2d测量)

---

## 数据采集

### 2D相机

- **功能**: 连接 2D 相机并采集 2D 图像。
- **输出**: 2D 图像 (灰度或彩色)

### 光源

- **功能**: 控制光源开关和亮度，配合 2D 相机获取高质量图像。

---

## 预处理 - 基本处理

### 图像预处理

- **功能**: 对输入图像进行增强、降噪、形态学变换、灰度反转、边缘提取等预处理操作。通过灵活组合多种预处理方法（至多10个），提升图像质量。
- **使用场景**: 需要提升图像质量、突出图像特征的场景。通常作为图像分析的前置流程。
- **参数**:
  - ROI设置: 限制预处理区域
  - 预处理方法 (可组合，建议控制在2~4个):

#### 图像增强方法

| 方法               | 说明                                       | 适用场景                                 |
| ------------------ | ------------------------------------------ | ---------------------------------------- |
| 对比度增强 (CLAHE) | 对比度受限自适应直方图均衡化，避免过度增强 | 图像发灰、局部对比度不足，尤其光照不均时 |
| 对比度增强 (锐化)  | 增强边缘和细节                             | 图像模糊                                 |
| 直方图均衡化       | 增强整体对比度，无需参数                   | 整体对比度不足                           |
| 伽马校正           | Gamma>1提亮暗部，Gamma<1抑制亮部           | 图像整体偏暗或偏亮                       |
| 线性变换           | 缩放因子调对比度，偏置调亮度               | 需同时调整对比度和亮度                   |

#### 图像降噪方法

| 方法     | 说明                                         | 适用场景                 |
| -------- | -------------------------------------------- | ------------------------ |
| 高斯滤波 | 抑制噪声同时保留边缘，参数: 窗口大小、标准差 | 需要后续边缘检测的场景   |
| 中值滤波 | 替换为邻域中值，参数: 核大小                 | 椒盐噪声（随机黑白点）   |
| 均值滤波 | 替换为邻域均值，参数: 核大小                 | 对边缘细节要求不高的平滑 |

#### 图像形态学变换方法

| 方法   | 说明                         | 适用场景             |
| ------ | ---------------------------- | -------------------- |
| 腐蚀   | 收缩目标区域，消除小噪点     | 目标边缘偏粗，需收缩 |
| 膨胀   | 扩大目标区域，填充小孔洞     | 目标偏细，局部连接弱 |
| 开运算 | 先腐蚀后膨胀，去小噪点保边缘 | 二值图边缘有小毛刺   |
| 闭运算 | 先膨胀后腐蚀，填小孔平滑边缘 | 二值图内部有小孔洞   |

#### 其他方法

| 方法          | 说明                          | 适用场景                            |
| ------------- | ----------------------------- | ----------------------------------- |
| 灰度反转      | 亮暗互换                      | 前景/背景灰度关系与后续处理要求相反 |
| Sobel边缘检测 | 快速粗略找边缘，对噪声敏感    | 快速粗定位或结果预览                |
| Canny边缘检测 | 精确边缘，参数: 强/弱边缘阈值 | 精确定位或测量                      |
| 边缘绘制检测  | 获取连贯平滑边缘              | 对边缘连续性要求高                  |

**方法选用指导**:
- 若叠加多个预处理方法仍效果不佳，优先优化图像采集条件
- 建议同时启用的方法控制在2~4个

---

### 图像二值化

- **功能**: 按设定方法对高于或低于阈值的像素分别进行二值化处理，将图像转为"非黑即白"。
- **使用场景**: 在 2D 图像上根据阈值分割像素。
- **输入**: 图像 (Image)
- **输出**: 处理后的图像 (Image) — 二值图
- **参数**:
  - 二值化方法:
  - 二值图反转: 对结果取反

#### 全局自适应阈值分割

自动计算全局阈值。适合**光照不均匀、背景渐变**的场景。

| 参数         | 说明                                   |
| ------------ | -------------------------------------- |
| 图像通道类型 | 灰度图、色相通道、饱和度通道、明度通道 |
| 阈值计算方法 | 均值 / 加权平均                        |
| 阈值分割类型 | 二值化 / 反二值化                      |
| 核大小       | 计算阈值的邻域范围 (默认21px)          |
| 常数         | 增大→整体变暗，减小→变亮 (默认0)       |

#### 双阈值分割

设定高/低阈值精确控制范围。适合**提取特定亮度范围**。

| 参数         | 说明                                             |
| ------------ | ------------------------------------------------ |
| 阈值1、阈值2 | 阈值1<阈值2时提取区间内；阈值1>阈值2时提取区间外 |

#### 动态阈值分割

分小区域单独计算阈值。适合**有噪声需去除**的场景。

| 参数         | 说明                           |
| ------------ | ------------------------------ |
| 阈值分割类型 | 亮区 / 暗区 / 范围内 / 范围外  |
| 滤波器类型   | 均值滤波 / 高斯滤波 / 中值滤波 |
| 像素值偏置   | 调整二值化宽松度               |
| 核大小       | 滤波窗口边长 (奇数)            |

#### 全局固定阈值分割

手动设定固定阈值。适合**背景干净、对比度高**的场景。

| 参数         | 说明                                    |
| ------------ | --------------------------------------- |
| 图像通道类型 | 灰度图、色相、饱和度、明度/亮度         |
| 阈值 (0~255) | 手动设定                                |
| 阈值分割类型 | 二值化 / 反二值化 / 截断阈值 / 归零阈值 |

---

### 图像形态学变换

- **功能**: 使用膨胀、腐蚀、开/闭运算、骨架化对图像去噪或填充。
- **使用场景**: 图像质量不佳需进一步调整时。
- **输入**: 图像 (Image/Color)
- **输出**: 处理后的图像 (Image/Color)
- **参数**:

| 操作类型 | 说明                             | 参数                                           |
| -------- | -------------------------------- | ---------------------------------------------- |
| 闭运算   | 先膨胀后腐蚀，填充小孔洞平滑边缘 | 操作核大小(默认21px)、核形状(矩形/十字形/椭圆) |
| 膨胀     | 填充小孔洞使图像完整             | 操作核大小(默认3px)、核形状                    |
| 腐蚀     | 消除小物体或噪点                 | 操作核大小(默认3px)、核形状                    |
| 开运算   | 先腐蚀后膨胀，去噪保边缘         | 操作核大小(默认21px)、核形状                   |
| 骨架化   | 提取掩膜骨架                     | 无                                             |

核形状说明: 矩形(常规) < 十字形(线性结构) < 椭圆(精细边缘), 细节还原程度递增，耗时递增。

---

### 图像算术运算

- **功能**: 对两张图像进行加、减、乘等算术运算。

### 拼接图像

- **功能**: 将多张图像拼接为一张。

---

## 预处理 - 颜色处理

### 转换图像色彩空间

- **功能**: 在不同色彩空间之间转换 (如 RGB→HSV、RGB→灰度等)。

### 通过颜色提取目标区域

- **功能**: 根据颜色范围从彩色图像中提取符合条件的目标区域 (掩膜输出)。

### 合并或拆分图像通道

- **功能**: 将多通道图像拆分为单通道，或将单通道合并为多通道。

---

## 预处理 - 掩膜

### 提取掩膜中对应的图像

- **功能**: 使用掩膜提取原始图像中对应区域的像素。

### 掩膜逻辑运算

- **功能**: 对多个掩膜进行与/或/非等逻辑运算。

---

## 预处理 - 空间变换

### 2D对齐

- **功能**: 根据对齐参数组对图像进行位姿变换 (平移+旋转)，使后续步骤的 ROI 跟随目标物体移动。

### 2D Blob对齐

- **功能**: 通过 Blob 分析结果自动计算对齐参数，实现目标物体的位姿对齐。

### 裁剪、填充或缩放图像

- **功能**: 裁剪指定区域、在图像周围填充像素、或按比例缩放图像。

### 旋转图像

- **功能**: 按指定角度旋转图像。

### 翻转与旋转图像

- **功能**: 水平翻转、垂直翻转或90°倍数旋转图像。

### 拉直环形图像区域

- **功能**: 将环形区域展开拉直为矩形图像 (适用于圆形工件周边检测)。

### 还原缩放后ROI内的图像

- **功能**: 将缩放后 ROI 区域内的图像还原到原始尺寸。

---

## 定位 - 2D几何特征

### 创建点（2D）

- **功能**: 在图像上创建一个指定坐标的 2D 点特征。

### 创建直线（2D）

- **功能**: 在图像上创建一条指定的 2D 直线特征。

### 定位掩膜特征点

- **功能**: 从掩膜图像中定位特征点 (如质心、最远点等)。

### 检测边缘点

- **功能**: 在图像指定区域的纵向中心线上检测出一个符合要求的边缘点。
- **使用场景**: 通用几何特征检测。
- **参数**:

| 参数         | 说明                                |
| ------------ | ----------------------------------- |
| ROI设置      | 绘制矩形ROI，从中心提取一列像素     |
| 滤波窗口大小 | 减少噪声，提升稳定性                |
| 灰度变化阈值 | 相邻像素灰度变化量≥该值才检测到边缘 |
| 边缘极性     | 暗到亮 / 亮到暗 / 任意              |

### 检测并拟合直线

- **功能**: 从图像中检测直线边缘并拟合直线。
- **使用场景**: 通用几何特征检测与拟合。
- **基本概念**:
  - **卡尺**: 虚拟测量工具，在特定区域检测边缘
  - **边缘极性**: 边缘灰度值变化方向
- **参数**:

| 参数         | 说明                    |
| ------------ | ----------------------- |
| 拟合线方向   | X为正/X为负/Y为正/Y为负 |
| 边缘极性     | 暗到亮 / 亮到暗 / 任意  |
| 滤波窗口大小 | 减少噪声                |
| 边缘类型     | 最优 / 首个 / 最后一个  |
| 灰度变化阈值 | 过滤微弱边缘和噪声      |
| 使用相对阈值 | 按最大变化量百分比判断  |
| 离群点百分比 | 拟合时剔除的离群点比例  |

### 检测并拟合圆

- **功能**: 从图像中检测圆形边缘并拟合圆。
- **使用场景**: 对包含圆形轮廓的工件或零部件进行自动检测、圆心定位、半径测量。
- **参数**: (与拟合直线类似)

| 参数         | 说明                   |
| ------------ | ---------------------- |
| 边缘极性     | 暗到亮 / 亮到暗 / 任意 |
| 滤波窗口大小 | 减少噪声               |
| 边缘类型     | 最优 / 首个 / 最后一个 |
| 灰度变化阈值 | 过滤微弱边缘           |
| 使用相对阈值 | 按百分比判断           |
| 离群点百分比 | 拟合时剔除离群点比例   |

### 检测并拟合矩形

- **功能**: 从图像中检测矩形边缘并拟合矩形。
- **使用场景**: 矩形工件定位和尺寸测量。

### 检测并拟合腰型孔

- **功能**: 从图像中检测腰型孔 (长圆孔/跑道形) 边缘并拟合。
- **使用场景**: 腰型孔特征检测与定位。

---

## 定位 - 2D定位

### 2D Blob分析

- **功能**: 从图像中检测 Blob (连通区域)，并根据几何特征 (面积、圆度等) 进行筛选。
- **使用场景**: 在 2D 图像中检出目标连通区域。
- **参数**:

#### 检测设置

| 参数         | 说明                                |
| ------------ | ----------------------------------- |
| ROI设置      | 限制处理区域                        |
| 掩膜设置     | 忽略被覆盖的区域                    |
| Blob极性     | 比背景暗 / 比背景亮                 |
| 阈值类型     | 手动 (设定固定阈值) / 自动          |
| 邻域类型     | 四邻域 (上下左右) / 八邻域 (含对角) |
| 轮廓检索模式 | 外部轮廓 / 所有轮廓 (含内部孔洞)    |

#### 过滤设置

支持按以下几何特征筛选 Blob:
- 面积、总面积
- 外接矩形宽度/高度/宽高比
- 主轴角度、圆度
- 外接矩形中心点 X/Y
- 内切圆半径、外接圆半径
- 内接矩形宽度/高度
- 质心 X/Y

条件间逻辑: AND / OR

#### 排序设置

| 参数     | 说明                   |
| -------- | ---------------------- |
| 排序依据 | 面积/圆度/中心点坐标等 |
| 排序方向 | 升序 / 降序            |
| Z形排序  | 可按行/列顺序排列      |

#### 输出设置

| 参数                 | 说明                   |
| -------------------- | ---------------------- |
| 输出结果数量上限     | 限制输出Blob数量       |
| 输出合并后的掩膜     | 输出合并后Blob掩膜图像 |
| 检测到Blob时输出True | 用于逻辑判断           |

#### 典型应用案例

| 场景             | 推荐做法                              |
| ---------------- | ------------------------------------- |
| 检测圆形零件     | Blob极性+手动阈值+圆度过滤+面积过滤   |
| 检测矩形零件     | 自动阈值+四邻域+宽高比过滤            |
| 检测小目标物体   | 高阈值下限+八邻域+面积+外接圆双条件   |
| 复杂背景目标检测 | ROI+掩膜+所有轮廓+AND多条件           |
| 精确控制输出数量 | 自动阈值+外部轮廓+限制输出数+面积降序 |

---

### 2D匹配

- **功能**: 在 2D 图像中搜索并定位与模板匹配的特征，计算物体位姿。支持多目标定位与识别。
- **使用场景**: 计算 2D 图像中目标物体位姿。
- **参数**:

#### 模板设置

| 参数     | 说明         |
| -------- | ------------ |
| 选择模板 | 设置匹配模板 |

#### 匹配设置 - 常用参数

| 参数             | 说明                              |
| ---------------- | --------------------------------- |
| 掩膜设置         | 忽略掩膜覆盖区域                  |
| 边缘极性敏感     | 是否要求极性与模板一致 (默认开启) |
| 匹配结果数量上限 | 输出的最大匹配数 (默认1)          |
| 最小匹配分数     | 低于此分数的结果丢弃 (默认50.0)   |

#### 匹配设置 - 高级参数

| 参数             | 说明                                     |
| ---------------- | ---------------------------------------- |
| 有效匹配阈值     | 梯度幅度≥该值的点视为有效边缘点 (默认10) |
| 有效匹配比例下限 | 有效边缘点占模板总数最小比例 (默认50%)   |
| 搜索半径         | 位姿修正时的搜索区域半径 (默认8)         |
| 重叠比例上限     | 过滤重复匹配 (默认50%)                   |
| 填充比例         | 目标超出图像边界时的填充尺寸比例         |

---

## 识别

### 一维码/二维码识别

- **功能**: 在指定图像区域内识别一维码/二维码，输出码的内容及位置。
- **支持码制**: UPC-A、UPC-E、EAN-8、EAN-13、Code 93、Code 39、Code 128、ITF、RSS-14、RSS Expanded、Codabar、QR Code、Data Matrix、Aztec Code、PDF417、MaxiCode
- **输入**:
  - 图像 (Image/Color)
  - 对齐参数组 (Pose2D，可选)
- **输出**:
  - 一维码/二维码内容 (String)
  - 一维码/二维码位置 (Shape2D/Contour)
  - 校验结果 (Bool，勾选结果校验后)
- **参数**:

| 参数     | 说明                                                            |
| -------- | --------------------------------------------------------------- |
| 解码策略 | 标准 (快速，适合清晰高对比码) / 鲁棒 (适合模糊、倾斜、复杂背景) |
| ROI设置  | 限制处理区域                                                    |
| 结果校验 | 防止误读                                                        |
| 校验模式 | 自定义 (固定内容) / 全局变量 (动态内容)                         |

---

## 后处理 - 2D分析

### 按灰度范围统计像素数量

- **功能**: 统计图像中指定灰度范围内的像素数量。
- **使用场景**: 缺陷面积估算、区域覆盖率检测。

### 灰度直方图分析

- **功能**: 分析图像灰度直方图，输出统计信息 (均值、标准差等)。
- **使用场景**: 图像质量评估、亮度监控。

---

## 测量 - 2D测量

### 测量边缘间宽度

- **功能**: 从图像中检测一对边缘点，测量它们之间的距离作为边缘间宽度。
- **使用场景**: 零件间隙检测、包装宽度测量等需要精准测量距离的场景。
- **输入**:
  - 图像 (Image)
  - 对齐参数组 (Pose2D，可选)
- **输出**:
  - 边缘连线 (Shape2D/Line)
  - 边缘间宽度 (Number)
- **参数**:

| 参数           | 说明                              |
| -------------- | --------------------------------- |
| ROI设置        | 矩形ROI，根据卡尺设置提取多列像素 |
| 滤波窗口大小   | 减少噪声                          |
| 灰度变化阈值   | 边缘检测灵敏度                    |
| 期望边缘间宽度 | 多对候选时选择最接近期望值的      |
| 上/下边缘极性  | 暗到亮 / 亮到暗 / 任意            |

### 将像素距离转换为实际距离

- **功能**: 根据标定信息将像素距离转换为实际物理距离 (mm)。
- **使用场景**: 2D测量结果需要输出真实尺寸时。

### 测量特征间的距离

- **功能**: 测量两类指定特征之间的几何距离。
- **支持特征组合**: 点点、点线、点圆、线线、线圆、圆圆
- **输入**:
  - 图像 (Image)
  - IN1 (Shape2D — 点/线/圆)
  - IN2 (Shape2D — 点/线/圆)
- **输出**:
  - 距离 (Number)
  - 距离线 (Shape2D/Line)

### 测量线段间夹角

- **功能**: 测量两条线段之间的夹角。
- **输入**:
  - 图像 (Image)
  - IN1 (Shape2D/Line)
  - IN2 (Shape2D/Line)
- **输出**:
  - 角度 (Number)
  - 角度弧 (Shape2D/Arc)
  - 线段交点 (Point2D)
- **参数**:
  - 输出补角: 是否同时输出 180°-夹角 (默认不勾选)

---

## 后处理 - 2D可视化

### 图像可视化（2D）

- **功能**: 将 2D 测量结果 (线段、点、圆、文字标注等) 叠加到图像上进行可视化展示。

---

## 工具步骤（通用，可用于 2D 流程）

### 逻辑判断

- **功能**: 对数值/布尔值进行逻辑判断，输出判定结果。

### 列表值判定

- **功能**: 对列表中的值逐一判定是否在范围内。

### 数据提取

- **功能**: 从列表中提取指定索引的数据。

### 合并数据

- **功能**: 将多个数据合并为列表。

### 数值运算

- **功能**: 对数值进行加减乘除等运算。

---

## 典型 2D 工程流程示例

```
2D相机 → 图像预处理 → 图像二值化 → 2D Blob分析 → 测量特征间距离 → 质量判定 → 通信输出
```

```
2D相机 → 图像预处理 → 检测并拟合直线 → 检测并拟合圆 → 测量线段间夹角 → 质量判定
```

```
2D相机 → 2D匹配(定位) → 2D对齐 → 测量边缘间宽度 → 像素转实际距离 → 通信输出
```

```
2D相机 → 图像预处理 → 一维码/二维码识别 → 结果校验 → 通信输出
```
